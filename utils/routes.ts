export const enum Routes {
  home = "/",
  products = "/products",
  customers = "/customers",
  docs = "/auth/docs",
  our_story = "/auth/our-story",
  pricing = "/pricing",
  contacts = "/contact",
  login = "/login",
  join_waiting = "/join",
}
